# =============================================================================
# 文件名:   modules/hardware/default.nix
# 功能描述: 硬件驱动模块聚合入口
# 说明:     导入所有硬件相关配置：显卡、音频、打印等
# =============================================================================
{ config, pkgs, ... }:

{
  imports = [
    ./nvidia.nix    # NVIDIA 显卡驱动
    ./audio.nix     # 音频（PipeWire）
    ./printing.nix  # 打印服务
  ];
}
