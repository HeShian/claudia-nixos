# =============================================================================
# 文件名:   modules/multimedia/default.nix
# 功能描述: 多媒体模块聚合入口
# 说明:     导入所有多媒体相关配置：OBS 录播、Flatpak、剪贴板管理
# =============================================================================
{ config, pkgs, ... }:

{
  imports = [
    ./obs.nix       # OBS Studio 录播软件
    ./flatpak.nix   # Flatpak 应用支持
    ./clipman.nix   # 剪贴板管理工具
  ];
}
