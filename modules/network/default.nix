# =============================================================================
# 文件名:   modules/network/default.nix
# 功能描述: 网络模块聚合入口
# 说明:     导入所有网络相关配置：镜像源、代理/VPN、远程连接工具
# =============================================================================
{ config, pkgs, ... }:

{
  imports = [
    ./vpn-proxy.nix     # VPN/代理（v2rayA + Clash Verge Rev）
    ./ssh-client.nix    # 远程连接工具
  ];
}
